class User:
	
	def __repr__(self):
		return self.username

	def __init__(self, username, IP):
		self.username = username
		self.ipaddress = IP

	def changeUsername(username);
		self.username = username
	
	def changeIP(IP):
		self.IP = IP
